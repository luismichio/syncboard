fix: don't clear Figma token on transient verify errors

- /api/figma/verify now returns 502 for timeouts and Figma 5xx (transient),
  only returns 401 when Figma explicitly rejects the token
- Plugin only clears Figma token on res.status === 401 from verify,
  preserving connected state through network glitches
