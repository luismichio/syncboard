figjam-mirror: full parity pass (all-copies, keep-size, scale/format, penpot, height)

- figjamPlace honours allCopies (update every instance of the key) and
  preserveSize (skip resize; FILL crop kept by design in FigJam)
- New figjam-set-meta message persists per-instance format/scale from the
  Sync card group controls; summaries now round-trip format/scale/platform
  so settings survive selection refresh; propagate extends to siblings
- renderNode/placeOnBoard parametrized by format + scale; sync pass renders
  with the card's format (png/svg)
- Penpot parity in the mirror: parsePenpotUrl, export_shape via the Penpot
  Companion relay, and detect-selection via callRelay ('select' action) —
  all were no-op stubs
- ui.html adds sb-editor-figjam root class (min-height 640px, iframe 100%)
  so the FigJam panel renders without a scrollbar, Figma editor untouched
- Figma detect-selection stays deferred to M3 pairing (relay-pull)

Test: yarn lint + yarn test (138) + tsc clean
